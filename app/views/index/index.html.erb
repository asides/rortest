<%= link_to 'Posts admin', posts_path %>
 <br><br>
<% @posts.each do |post| %>
<div class="<%= 'red-border' if post.is_default %>">
<p>
  <strong>Title:</strong>
  <%= post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= post.body %>
</p>
</div>
<% end %>